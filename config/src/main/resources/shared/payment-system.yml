spring:
  datasource:
    url: 'jdbc:h2:mem:payment_system'
    username: 'sa'
    password:
    driver-class-name: 'org.h2.Driver'

  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'

  hibernate:
    ddl-auto: 'create'

logging:
  file: 'logs/app.log'